import 'package:flutter/material.dart';

abstract class AppColors {
  static const Color background = Color(0xFF303030);
  static const Color buttonBackground = Color(0xFF424242);
  static const Color buttonForeground = Colors.white;
  static const Color backButtonBackground = Colors.black54;
}